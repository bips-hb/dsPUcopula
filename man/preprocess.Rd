% Generated by hand from R/server_funs.R
\name{preprocessDataDS}
\alias{save_original_varnamesDS}
\alias{save_original_classesDS}
\alias{preprocessDataDS}
\alias{postprocessDataDS}
\title{Helpers for preserving and restoring server-side metadata}
\description{
Functions that capture the original structure of the training data, prepare it
for copula fitting and later restore the factor levels of the synthetic
outputs.
}
\usage{
save_original_varnamesDS(data_str)

save_original_classesDS(data_str)

preprocessDataDS(data_str)

postprocessDataDS(data_str, cat_dummy_levels_str)
}
\arguments{
  \item{data_str}{Character name of the data object stored on the DataSHIELD
  server.}
  \item{cat_dummy_levels_str}{Character name of the metadata object returned by
  \code{preprocessDataDS()}.}
}
\value{
  \code{save_original_varnamesDS()} returns a character vector of column names.
  \code{save_original_classesDS()} returns the associated classes.\newline
  \code{preprocessDataDS()} returns a list containing the processed data set and
  metadata describing factor levels.\newline
  \code{postprocessDataDS()} returns a \code{data.frame} with factor variables
  reconstructed from dummy encodings.
}
